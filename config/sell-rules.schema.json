{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Artifacts MMO Bot Sell Rules",
  "description": "Controls which items get sold on the Grand Exchange and how they're priced.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },

    "sellDuplicateEquipment": {
      "type": "boolean",
      "default": true,
      "description": "Enable claim-aware duplicate gear disposal. Craftable surplus gear can be recycled, and GE selling uses the same logic for sellable equipment including dropped loot."
    },

    "alwaysSell": {
      "type": "array",
      "default": [],
      "description": "Items to sell whenever they are found in bank. These rules override normal keep logic for the same item code, except neverSell.",
      "items": {
        "type": "object",
        "required": ["code"],
        "additionalProperties": false,
        "properties": {
          "code": {
            "type": "string",
            "description": "Item code to sell (e.g. 'copper_ore', 'ash_plank')"
          },
          "keepInBank": {
            "type": "integer",
            "default": 0,
            "minimum": 0,
            "description": "Quantity to keep in bank before selling the rest"
          }
        }
      }
    },

    "neverSell": {
      "type": "array",
      "default": [],
      "description": "Item codes that should never be sold, even if they match other rules",
      "items": {
        "type": "string",
        "description": "Item code (e.g. 'tasks_coin')"
      }
    },

    "pricingStrategy": {
      "type": "string",
      "enum": ["undercut"],
      "default": "undercut",
      "description": "Pricing strategy. Currently only 'undercut' is implemented — queries GE listings and undercuts the lowest price."
    },
    "undercutPercent": {
      "type": "number",
      "default": 5,
      "minimum": 0,
      "maximum": 100,
      "description": "Percentage to undercut the lowest GE listing by (e.g. 1 = 1% below lowest)"
    },
    "minPrice": {
      "type": "integer",
      "default": 1,
      "minimum": 1,
      "description": "Minimum sell price floor (never list below this)"
    },

    "cancelStaleAfterHours": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "description": "Cancel unsold GE orders older than this many hours. 0 = never cancel."
    }
  }
}
