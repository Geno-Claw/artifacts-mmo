# === Required ===
ARTIFACTS_TOKEN=your_jwt_token_here
ARTIFACTS_API=https://api.artifactsmmo.com
CHARACTER_NAME=GenoClaw
WEBSOCKET_URL=wss://realtime.artifactsmmo.com

# === Dashboard ===
# Base path for reverse proxy (e.g. Tailscale Serve at /artifacts)
DASHBOARD_BASE_PATH=/artifacts
# Host and port for the dashboard server
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=8091

# === Optional ===
# Path to bot config JSON (default: ./config/characters.json)
# BOT_CONFIG=./config/characters.json
# Path to persistent order board state
# ORDER_BOARD_PATH=./data/order-board.json
# Path to persistent gear state
# GEAR_STATE_PATH=./data/gear-state.json
# Graceful shutdown timeout in ms (default: 120000)
# RUNTIME_STOP_TIMEOUT_MS=120000
